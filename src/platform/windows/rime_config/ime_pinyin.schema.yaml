# ime_pinyin.schema.yaml
# 跨平台输入法 - 简体拼音方案
# 基于雾凇拼音 (rime-ice) 定制

schema:
  schema_id: ime_pinyin
  name: 简体拼音
  version: "1.0.0"
  author:
    - 跨平台输入法项目
  description: |
    基于雾凇拼音的简体中文拼音输入方案
    支持全拼输入，智能词频排序
  dependencies:
    - melt_eng  # 英文混输

switches:
  - name: ascii_mode
    reset: 0
    states: [ 中, 英 ]
  - name: full_shape
    states: [ 半, 全 ]
  - name: ascii_punct
    states: [ 。, . ]
  - name: emoji
    reset: 1
    states: [ 💀, 😐 ]

engine:
  processors:
    - ascii_composer
    - recognizer
    - key_binder
    - speller
    - punctuator
    - selector
    - navigator
    - express_editor
  segmentors:
    - ascii_segmentor
    - matcher
    - abc_segmentor
    - punct_segmentor
    - fallback_segmentor
  translators:
    - punct_translator
    - script_translator
    - table_translator@melt_eng
  filters:
    - simplifier@emoji
    - uniquifier

speller:
  alphabet: zyxwvutsrqponmlkjihgfedcba
  delimiter: " '"
  algebra:
    - erase/^xx$/
    # 模糊音规则（可选）
    # - derive/^([zcs])h/$1/
    # - derive/^([zcs])([^h])/$1h$2/
    # - derive/([aei])n$/$1ng/
    # - derive/([aei])ng$/$1n/
    # - derive/^l/n/
    # - derive/^n/l/
    # 简拼支持
    - abbrev/^([a-z]).+$/$1/
    - abbrev/^([zcs]h).+$/$1/

translator:
  dictionary: ime_pinyin
  prism: ime_pinyin
  preedit_format:
    - xform/([nl])v/$1ü/
    - xform/([nl])ue/$1üe/
    - xform/([jqxy])v/$1u/
  initial_quality: 1.2

# 英文混输
melt_eng:
  dictionary: melt_eng
  enable_completion: true
  enable_sentence: false
  initial_quality: 0.8
  comment_format:
    - xform/.*//

# Emoji 表情
emoji:
  opencc_config: emoji.json
  option_name: emoji
  tips: none

# 标点符号
punctuator:
  import_preset: default

# 快捷键
key_binder:
  import_preset: default
  bindings:
    # 以词定字
    - { when: has_menu, accept: bracketleft, send: "[" }
    - { when: has_menu, accept: bracketright, send: "]" }

# 识别器
recognizer:
  import_preset: default
  patterns:
    # 网址
    url: "^(www[.]|https?:|ftp[.:]|mailto:|file:).*$|^[a-z]+[.].+$"
    # 邮箱
    email: "^[A-Za-z][-_.0-9A-Za-z]*@.*$"
    # 大写开头自动切换英文
    uppercase: "[A-Z][-_+.'0-9A-Za-z]*$"
